services:
  frontend:
    container_name: mira-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod

    ports:
      - "80:80"

  backend:
    container_name: mira-backend
    build:
      context: ./backend
      dockerfile: Dockerfile.prod

    ports:
      - "3000:3000"